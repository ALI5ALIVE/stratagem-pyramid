

# Update CoAnalyst & Module Card Labels

## Changes across 3 files

### 1. Remove CoAuthor/CoTrainer references from CoAnalyst Intelligence Layer description
In all three platform slides, update the CoAnalyst card description to remove "through CoAuthor, CoAnalyst, and CoTrainer" phrasing.

**Files**: `ExecSlide3Platform.tsx`, `TechSlide4Platform.tsx`, `OpsSlide4Platform.tsx`

### 2. Add "+ CoAuthor" to Content Manager card name
Update the `name` field for Content Manager from `"Content Manager365"` to `"Content Manager365 + CoAuthor"` in all three files.

### 3. Add "+ CoTrainer" to Training Manager card name
Update the `name` field for Training Manager from `"Training Manager365"` to `"Training Manager365 + CoTrainer"` in all three files.

### Summary of text changes

| Location | Before | After |
|----------|--------|-------|
| CoAnalyst card description | "...through CoAuthor, CoAnalyst, and CoTrainer" | Remove that clause |
| Content Manager name | "Content Manager365" | "Content Manager365 + CoAuthor" |
| Training Manager name | "Training Manager365" | "Training Manager365 + CoTrainer" |

