

# Rename DTOP Slide Title

## Change
In `src/components/tech-slides/TechSlide5DTOP.tsx` line 21, change the `title` prop from `"Platform Architecture: DTOP"` to `"Operating Model: DTOP"`.

Also update the slide label in `src/pages/TechnicalDeepDive.tsx` from `"DTOP Architecture"` to `"DTOP Operating Model"` for sidebar consistency.

