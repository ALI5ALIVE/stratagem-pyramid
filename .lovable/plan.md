## Update the Sell the Whole Vision — Whiteboard narration

Rewrite the `se-w3-whole-vision-whiteboard` script in `src/data/salesEnablementNarration.ts` so it matches the redesigned board:

- Open by pointing at the **MAD use-case strip at the top** (Detect → Trigger → Orchestrate → Prove micro-cards) — that's the worked example everything underneath proves.
- Walk **five beats bottom-up** to mirror the new sub-boxes:
  1. **Core Apps** — SafetyManager365 · ContentManager365 · TrainingManager365 (one signal, three systems of record)
  2. **Insights (Detect, blue)** — trend surfaces itself, no analyst pulling reports → MAD day 1
  3. **Intelligence Layer (Trigger, amber)** — ~90% vs ~35%, cited Recommended Actions → MAD day 2
  4. **Automation + Unified Mobile (Orchestrate, violet)** — drafts revision, opens reviews, assigns training, lands on crew devices in 48h → MAD day 3
  5. **Insights · Prove (green)** — trend flat, audit pack one click → MAD day 5
- Close verbatim: "one foundation, one intelligence, one loop — every layer a deal on its own."
- Drop the merged "Insights and the Intelligence Layer" wording and the implicit Orchestrate-and-Prove pairing, since the board now separates them.
- Keep length, tone, and the hand-off line to the Recap slide.

No other slides touched.