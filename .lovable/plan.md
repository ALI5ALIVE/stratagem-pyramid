## Tighten the whiteboard narration

Edit only `se-w3-whole-vision-whiteboard` in `src/data/salesEnablementNarration.ts`:

1. **Don't name the three apps.** Replace "SafetyManager365, ContentManager365, TrainingManager365 — one signal, three systems of record, visible to all three." with "the three apps as the foundation — one signal, visible across all three."
2. **Explicitly tag each layer to its DTOP step** after the Core Apps beat, in this exact phrasing:
   - "Two — Insights as Detect…"
   - "Three — the Intelligence Layer as Trigger…"
   - "Four — Automation as Orchestrate…" (Unified Mobile stays grouped here)
   - "Five — Insights as Prove…"
3. Keep the MAD top-of-board opening, the verbatim core line, and the verbatim close unchanged.

No other slides or files touched.