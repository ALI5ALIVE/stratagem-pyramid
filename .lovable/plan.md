

# Rename "How It Works" → "The New Operating Model" (Executive Pitch only)

Two string changes:

1. **`src/components/exec-slides/ExecSlide3DTOP.tsx`** — change `title="How It Works"` to `title="The New Operating Model"`
2. **`src/pages/ExecutivePitch.tsx`** — change `label: "How It Works"` to `label: "The New Operating Model"` in the slides array

