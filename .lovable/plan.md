The new film (`/mnt/documents/silos-to-signals.mp4`, 7.79 MB, dated May 24 06:04) hasn't replaced the old one in `public/keynote/silos-to-signals.mp4` (7.15 MB, dated May 24 05:22). The page is already pointing at `?v=3`, so it's just serving the stale file.

### Change
Copy `/mnt/documents/silos-to-signals.mp4` → `public/keynote/silos-to-signals.mp4` (overwrite).

That's it — no code changes needed; the `?v=3` cache-buster is already in place.