Remove three cards from the Command Centre "Additional Resources" section on `/` (`src/pages/HomePage.tsx`):

1. Market Development Hub
2. Positioning & Messaging Playbook
3. Category Research Programme

These live in the `additionalResources` array. Keep CEO Overview and Long — Technical Deep Dive. No other changes — sidebar and Market Development hub itself remain untouched (cards still reachable via sidebar).

File: `src/pages/HomePage.tsx`.
